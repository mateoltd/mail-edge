## API Report File for "@mail-edge/blob-s3"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { BlobStagePort } from '@mail-edge/core';
import type { BlobStageReservation } from '@mail-edge/core';
import { BlobStageWriter } from '@mail-edge/core';
import { BlobStorePort } from '@mail-edge/core';
import { KMSClient } from '@aws-sdk/client-kms';
import { S3Client } from '@aws-sdk/client-s3';

// @public (undocumented)
export interface AbandonedBlobStage {
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly objectKey: string;
    // (undocumented)
    readonly objectVersion?: string;
    // (undocumented)
    readonly stageId: string;
    // (undocumented)
    readonly state: "abandoned" | "promoted";
    // (undocumented)
    readonly tenantId: BlobTenantId;
}

// @public (undocumented)
export interface AwsKmsEnvelopeKeyConfig {
    // (undocumented)
    readonly keyReference: string;
    // (undocumented)
    readonly operationTimeoutMilliseconds?: number;
}

// @public
export class AwsKmsEnvelopeKeyService implements EnvelopeKeyService {
    constructor(client: KMSClient, config: AwsKmsEnvelopeKeyConfig);
    // (undocumented)
    generate(context: Parameters<EnvelopeKeyService["generate"]>[0], signal: AbortSignal): Promise<EnvelopeKey>;
    // (undocumented)
    unwrap(wrappedKey: Uint8Array, keyReference: string, context: Parameters<EnvelopeKeyService["unwrap"]>[2], signal: AbortSignal): Promise<Uint8Array>;
}

// @public (undocumented)
export interface BlobClock {
    // (undocumented)
    now(): string;
}

// @public (undocumented)
export interface BlobErrorFactory {
    // (undocumented)
    create(input: {
        readonly operation: string;
        readonly message: string;
        readonly retryable: boolean;
        readonly code?: "INGRESS_LIMIT_EXCEEDED" | "NOT_FOUND" | "STORAGE_UNAVAILABLE" | "CONFLICT" | "VALIDATION_FAILED";
        readonly cause?: unknown;
    }): BlobFailure;
}

// Warning: (ae-forgotten-export) The symbol "ResultError" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type BlobFailure = ResultError<Awaited<ReturnType<BlobStageWriter["abort"]>>>;

// @public (undocumented)
export interface BlobFinalObject {
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly finalObjectKey: string;
    // (undocumented)
    readonly finalObjectVersion: string;
    // (undocumented)
    readonly stageId: string;
    // (undocumented)
    readonly tenantId: BlobTenantId;
}

// @public (undocumented)
export type BlobId = Parameters<BlobStorePort["getAvailableReference"]>[1];

// @public (undocumented)
export interface BlobIdGenerator {
    // (undocumented)
    next(): string;
}

// @public
export interface BlobMetadataStore {
    // (undocumented)
    abandonStage(tenantId: BlobTenantId, stageId: string, expectedVersion: number, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
    // (undocumented)
    claimExpiredStages(tenantId: BlobTenantId, expiredAt: string, limit: number, signal: AbortSignal): Promise<DriverResult<readonly AbandonedBlobStage[]>>;
    // (undocumented)
    claimOrphanPurge(tenantId: BlobTenantId, blobId: string, deletionId: string, now: string, leaseMilliseconds: number, signal: AbortSignal): Promise<DriverResult<BlobPurgeClaim>>;
    // (undocumented)
    claimRetentionPurge(tenantId: BlobTenantId, blobId: string, deletionId: string, now: string, leaseMilliseconds: number, signal: AbortSignal): Promise<DriverResult<BlobPurgeClaim>>;
    // (undocumented)
    commitPromotion(input: BlobPromotionCommit, signal: AbortSignal): Promise<DriverResult<StoredBlobRecord>>;
    // (undocumented)
    completePurge(claim: BlobPurgeClaim, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
    // (undocumented)
    completeStageCleanup(stage: AbandonedBlobStage, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
    // (undocumented)
    getBlob(tenantId: BlobTenantId, blobId: string, signal: AbortSignal): Promise<DriverResult<StoredBlobRecord>>;
    // (undocumented)
    listPendingPromotions(tenantId: BlobTenantId, limit: number, signal: AbortSignal): Promise<DriverResult<readonly PendingBlobPromotion[]>>;
    // (undocumented)
    listRetentionCandidates(tenantId: BlobTenantId, now: string, limit: number, signal: AbortSignal): Promise<DriverResult<readonly string[]>>;
    // (undocumented)
    markCorrupt(claim: RawBlobIntegrityClaim, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
    // (undocumented)
    markObjectDeleted(claim: BlobPurgeClaim, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
    // (undocumented)
    markUploaded(input: BlobStageUpload, occurredAt: string, signal: AbortSignal): Promise<DriverResult<{
        readonly optimisticVersion: number;
    }>>;
    // (undocumented)
    markUploading(tenantId: BlobTenantId, stageId: string, expectedVersion: number, occurredAt: string, signal: AbortSignal): Promise<DriverResult<{
        readonly optimisticVersion: number;
    }>>;
    // (undocumented)
    markVerified(tenantId: BlobTenantId, stageId: string, expectedVersion: number, occurredAt: string, signal: AbortSignal): Promise<DriverResult<{
        readonly optimisticVersion: number;
    }>>;
    // (undocumented)
    observeOrphans(tenantId: BlobTenantId, olderThan: string, observedAt: string, minimumObservationIntervalMilliseconds: number, limit: number, signal: AbortSignal): Promise<DriverResult<readonly string[]>>;
    // (undocumented)
    preparePromotion(input: BlobPromotionPreparation, occurredAt: string, signal: AbortSignal): Promise<DriverResult<{
        readonly optimisticVersion: number;
    }>>;
    // (undocumented)
    reclaimExpiredPurges(tenantId: BlobTenantId, now: string, leaseMilliseconds: number, limit: number, signal: AbortSignal): Promise<DriverResult<readonly BlobPurgeClaim[]>>;
    // (undocumented)
    recordFinalObject(input: BlobFinalObject, occurredAt: string, signal: AbortSignal): Promise<DriverResult<{
        readonly optimisticVersion: number;
    }>>;
    // (undocumented)
    replaceMissingFinalObject(input: BlobFinalObject & {
        readonly missingFinalObjectVersion?: string;
    }, occurredAt: string, signal: AbortSignal): Promise<DriverResult<{
        readonly optimisticVersion: number;
    }>>;
    // (undocumented)
    reserveStage(input: BlobStageCreation, signal: AbortSignal): Promise<DriverResult<{
        readonly optimisticVersion: number;
    }>>;
    // (undocumented)
    restoreCorrupt(proof: RawBlobRestorationProof, restoredAt: string, retainUntil: string, signal: AbortSignal): Promise<DriverResult<StoredBlobRecord>>;
    // (undocumented)
    revalidatePurgeClaim(claim: BlobPurgeClaim, now: string, signal: AbortSignal): Promise<DriverResult<void>>;
}

// @public
export class BlobOrphanReaper {
    constructor(input: {
        readonly blobStore: PurgingBlobStore;
        readonly clock: BlobClock;
        readonly config: BlobOrphanReaperConfig;
        readonly errors: BlobErrorFactory;
        readonly ids: BlobIdGenerator;
        readonly metadata: BlobMetadataStore;
    });
    // (undocumented)
    runTenant(tenantId: BlobTenantId, signal: AbortSignal): Promise<DriverResult<readonly string[]>>;
}

// @public (undocumented)
export interface BlobOrphanReaperConfig {
    // (undocumented)
    readonly batchSize: number;
    // (undocumented)
    readonly graceMilliseconds: number;
    // (undocumented)
    readonly observationIntervalMilliseconds: number;
    // (undocumented)
    readonly purgeLeaseMilliseconds: number;
}

// @public (undocumented)
export type BlobOrphanReaperFailure = BlobFailure;

// @public (undocumented)
export interface BlobPromotionCommit {
    // (undocumented)
    readonly availableAt: string;
    // (undocumented)
    readonly blobId: string;
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly finalObjectKey: string;
    // (undocumented)
    readonly finalObjectVersion?: string;
    // (undocumented)
    readonly retainUntil: string;
    // (undocumented)
    readonly stageId: string;
    // (undocumented)
    readonly tenantId: BlobTenantId;
}

// @public (undocumented)
export interface BlobPromotionPreparation {
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly finalObjectKey: string;
    // (undocumented)
    readonly stageId: string;
    // (undocumented)
    readonly tenantId: BlobTenantId;
}

// @public
export class BlobPromotionRepairWorker {
    constructor(metadata: BlobMetadataStore, blobs: PromotionRepairStore, batchSize: number);
    // (undocumented)
    runTenant(tenantId: BlobTenantId, signal: AbortSignal): Promise<DriverResult<readonly StoredBlobRecord[]>>;
}

// @public (undocumented)
export interface BlobPurgeClaim {
    // (undocumented)
    readonly blobId: string;
    // (undocumented)
    readonly claimedUntil: string;
    // (undocumented)
    readonly deletionId: string;
    // (undocumented)
    readonly fence: number;
    // (undocumented)
    readonly objectKey: string;
    // (undocumented)
    readonly objectVersion?: string;
    // (undocumented)
    readonly tenantId: BlobTenantId;
}

// @public (undocumented)
export type BlobReservation = BlobStageReservation;

// @public
export class BlobRetentionWorker {
    constructor(input: {
        readonly blobStore: PurgingBlobStore_2;
        readonly clock: BlobClock;
        readonly config: BlobRetentionWorkerConfig;
        readonly errors: BlobErrorFactory;
        readonly ids: BlobIdGenerator;
        readonly metadata: BlobMetadataStore;
    });
    // (undocumented)
    runTenant(tenantId: BlobTenantId, signal: AbortSignal): Promise<DriverResult<readonly string[]>>;
}

// @public (undocumented)
export interface BlobRetentionWorkerConfig {
    // (undocumented)
    readonly batchSize: number;
    // (undocumented)
    readonly purgeLeaseMilliseconds: number;
}

// @public (undocumented)
export interface BlobStageCleanupConfig {
    // (undocumented)
    readonly batchSize: number;
    // (undocumented)
    readonly maximumListPages: number;
    // (undocumented)
    readonly operationTimeoutMilliseconds?: number;
}

// @public
export class BlobStageCleanupWorker {
    constructor(input: {
        readonly bucket: string;
        readonly clock: BlobClock;
        readonly config: BlobStageCleanupConfig;
        readonly errors: BlobErrorFactory;
        readonly metadata: BlobMetadataStore;
        readonly s3: S3Client;
    });
    // (undocumented)
    runTenant(tenantId: BlobTenantId, signal: AbortSignal): Promise<DriverResult<readonly string[]>>;
}

// @public (undocumented)
export interface BlobStageCreation {
    // (undocumented)
    readonly encryptionMetadata: Readonly<Record<string, unknown>>;
    // (undocumented)
    readonly expectedMaximumBytes: number;
    // (undocumented)
    readonly expiresAt: string;
    // (undocumented)
    readonly kmsKeyRef: string;
    // (undocumented)
    readonly objectKey: string;
    // (undocumented)
    readonly purpose: BlobReservation["purpose"];
    // (undocumented)
    readonly stageId: string;
    // (undocumented)
    readonly tenantId: BlobTenantId;
    // (undocumented)
    readonly wrappedDek: Uint8Array;
}

// @public (undocumented)
export interface BlobStageUpload {
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly objectVersion?: string;
    // (undocumented)
    readonly observedBytes: number;
    // (undocumented)
    readonly observedSha256: string;
    // (undocumented)
    readonly stageId: string;
    // (undocumented)
    readonly tenantId: BlobTenantId;
}

// @public (undocumented)
export type BlobTenantId = Parameters<BlobStorePort["getAvailableReference"]>[0];

// @public (undocumented)
export const DEFAULT_ENCRYPTION_FRAME_BYTES: number;

// @public (undocumented)
export const defaultBlobRetentionWorkerConfig: Readonly<BlobRetentionWorkerConfig>;

// @public (undocumented)
export const defaultBlobStageCleanupConfig: Readonly<BlobStageCleanupConfig>;

// @public
export const defaultEncryptedS3BlobStoreConfig: (bucket: string, keyPrefix: string) => EncryptedS3BlobStoreConfig;

// @public (undocumented)
export type DriverResult<T> = {
    readonly ok: true;
    readonly value: T;
} | {
    readonly ok: false;
    readonly error: BlobFailure;
};

// @public (undocumented)
export const encryptedFormatHeaderBytes = 48;

// @public
export class EncryptedS3BlobStagePort implements BlobStagePort {
    constructor(input: {
        readonly s3: S3Client;
        readonly keyService: EnvelopeKeyService;
        readonly metadata: BlobMetadataStore;
        readonly clock: BlobClock;
        readonly errors: BlobErrorFactory;
        readonly config: EncryptedS3BlobStoreConfig;
    });
    // (undocumented)
    reserve(reservation: BlobReservation, signal: AbortSignal): Promise<DriverResult<BlobStageWriter>>;
}

// @public
export class EncryptedS3BlobStore implements BlobStorePort {
    constructor(input: {
        readonly s3: S3Client;
        readonly keyService: EnvelopeKeyService;
        readonly metadata: BlobMetadataStore;
        readonly clock: BlobClock;
        readonly errors: BlobErrorFactory;
        readonly config: EncryptedS3BlobStoreConfig;
    });
    // (undocumented)
    getAvailableReference(tenantId: BlobTenantId, blobId: BlobId, signal: AbortSignal): ReturnType<BlobStorePort["getAvailableReference"]>;
    // (undocumented)
    openRaw(tenantId: BlobTenantId, blobId: BlobId, signal: AbortSignal): ReturnType<BlobStorePort["openRaw"]>;
    // (undocumented)
    purge(claim: BlobPurgeClaim, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
    // (undocumented)
    repairPromotion(pending: PendingBlobPromotion, signal: AbortSignal): Promise<DriverResult<StoredBlobRecord>>;
    // (undocumented)
    restoreCorrupt(tenantId: BlobTenantId, blobId: BlobId, expectedVersion: number, retainUntil: string, signal: AbortSignal): Promise<DriverResult<StoredBlobRecord>>;
    // (undocumented)
    readonly stages: EncryptedS3BlobStagePort;
}

// @public (undocumented)
export interface EncryptedS3BlobStoreConfig {
    // (undocumented)
    readonly bucket: string;
    // (undocumented)
    readonly cleanupTimeoutMilliseconds: number;
    // (undocumented)
    readonly encryptionFrameBytes: number;
    // (undocumented)
    readonly keyPrefix: string;
    // (undocumented)
    readonly maximumRawMessageBytes?: number;
    // (undocumented)
    readonly multipartPartBytes: number;
    // (undocumented)
    readonly multipartQueueSize: number;
    // (undocumented)
    readonly operationTimeoutMilliseconds?: number;
    // (undocumented)
    readonly rawRetentionMilliseconds: number;
    // (undocumented)
    readonly requireObjectVersion: boolean;
    // (undocumented)
    readonly scratchLifetimeMilliseconds: number;
    // (undocumented)
    readonly serverSideEncryption?: "AES256" | "aws:kms";
    // (undocumented)
    readonly serverSideEncryptionKmsKeyId?: string;
}

// @public (undocumented)
export const ENCRYPTION_FORMAT_VERSION = 1;

// @public (undocumented)
export interface EnvelopeKey {
    // (undocumented)
    readonly keyReference: string;
    // (undocumented)
    readonly plaintextKey: Uint8Array;
    // (undocumented)
    readonly wrappedKey: Uint8Array;
}

// @public (undocumented)
export interface EnvelopeKeyService {
    // (undocumented)
    generate(context: {
        readonly tenantId: BlobTenantId;
        readonly blobId: string;
        readonly purpose: BlobReservation["purpose"];
        readonly formatVersion: number;
    }, signal: AbortSignal): Promise<EnvelopeKey>;
    // (undocumented)
    unwrap(wrappedKey: Uint8Array, keyReference: string, context: {
        readonly tenantId: BlobTenantId;
        readonly blobId: string;
        readonly purpose: BlobReservation["purpose"];
        readonly formatVersion: number;
    }, signal: AbortSignal): Promise<Uint8Array>;
}

// @public (undocumented)
export interface PendingBlobPromotion {
    // (undocumented)
    readonly blobId: string;
    // (undocumented)
    readonly encryptionFormatVersion: number;
    // (undocumented)
    readonly encryptionMetadata: Readonly<Record<string, unknown>>;
    // (undocumented)
    readonly expectedSha256: string;
    // (undocumented)
    readonly expectedSize: number;
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly finalObjectKey: string;
    // (undocumented)
    readonly finalObjectVersion?: string;
    // (undocumented)
    readonly kmsKeyRef: string;
    // (undocumented)
    readonly purpose: BlobReservation["purpose"];
    // (undocumented)
    readonly scratchObjectKey: string;
    // (undocumented)
    readonly scratchObjectVersion?: string;
    // (undocumented)
    readonly stageId: string;
    // (undocumented)
    readonly tenantId: BlobTenantId;
    // (undocumented)
    readonly wrappedDek: Uint8Array;
}

// @public (undocumented)
export interface PromotionRepairStore {
    // (undocumented)
    repairPromotion(pending: PendingBlobPromotion, signal: AbortSignal): Promise<DriverResult<StoredBlobRecord>>;
}

// @public (undocumented)
interface PurgingBlobStore extends BlobStorePort {
    // (undocumented)
    purge(claim: BlobPurgeClaim, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
}

// @public (undocumented)
interface PurgingBlobStore_2 extends BlobStorePort {
    // (undocumented)
    purge(claim: BlobPurgeClaim, occurredAt: string, signal: AbortSignal): Promise<DriverResult<void>>;
}

// @public (undocumented)
export interface RawBlobIntegrityClaim {
    // (undocumented)
    readonly blobId: string;
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly tenantId: BlobTenantId;
}

// @public
export interface RawBlobRestorationProof {
    // (undocumented)
    readonly blobId: string;
    // (undocumented)
    readonly encryptionFormatVersion: number;
    // (undocumented)
    readonly encryptionHeaderSha256: string;
    // (undocumented)
    readonly expectedVersion: number;
    // (undocumented)
    readonly objectKey: string;
    // (undocumented)
    readonly objectVersion?: string;
    // (undocumented)
    readonly sha256: string;
    // (undocumented)
    readonly size: number;
    // (undocumented)
    readonly tenantId: BlobTenantId;
    // (undocumented)
    readonly verifiedAt: string;
}

// @public (undocumented)
type ResultError<Value> = Value extends {
    readonly ok: false;
    readonly error: infer Error;
} ? Error : never;

// @public (undocumented)
type ResultValue<Value> = Value extends {
    readonly ok: true;
    readonly value: infer Result;
} ? Result : never;

// @public (undocumented)
export interface StoredBlobRecord {
    // (undocumented)
    readonly encryptionFormatVersion: number;
    // (undocumented)
    readonly encryptionMetadata: Readonly<Record<string, unknown>>;
    // (undocumented)
    readonly kmsKeyRef: string;
    // (undocumented)
    readonly objectKey: string;
    // (undocumented)
    readonly objectVersion?: string;
    // (undocumented)
    readonly optimisticVersion: number;
    // (undocumented)
    readonly purpose: BlobReservation["purpose"];
    // Warning: (ae-forgotten-export) The symbol "ResultValue" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly raw: ResultValue<Awaited<ReturnType<BlobStorePort["getAvailableReference"]>>>;
    // (undocumented)
    readonly sourceStageId: string;
    // (undocumented)
    readonly status: "available" | "purge_pending" | "deleted" | "corrupt";
    // (undocumented)
    readonly tenantId: BlobTenantId;
    // (undocumented)
    readonly wrappedDek: Uint8Array;
}

// @public
export const storedObjectIdentity: (record: StoredBlobRecord) => Readonly<{
    key: string;
    version?: string;
}>;

// Warnings were encountered during analysis:
//
// src/orphan-reaper.worker.ts:41:5 - (ae-forgotten-export) The symbol "PurgingBlobStore" needs to be exported by the entry point index.d.ts
// src/retention.worker.ts:38:5 - (ae-forgotten-export) The symbol "PurgingBlobStore_2" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
