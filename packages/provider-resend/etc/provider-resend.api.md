## API Report File for "@mail-edge/provider-resend"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { AttemptId } from '@mail-edge/provider';
import type { BlobStagePort } from '@mail-edge/provider';
import type { Clock } from '@mail-edge/provider';
import { ControlPlaneOperationContext } from '@mail-edge/provider';
import { FeedbackProviderAdapter } from '@mail-edge/provider';
import type { InboundIngressCommit } from '@mail-edge/provider';
import { InboundProviderAdapter } from '@mail-edge/provider';
import { InboundRawAcquirer } from '@mail-edge/provider';
import { MailEdgeError } from '@mail-edge/provider';
import { NormalizedEvidence } from '@mail-edge/provider';
import { OutboundProviderAdapter } from '@mail-edge/provider';
import { ProviderAdapterRegistration } from '@mail-edge/provider';
import { ProviderCapabilityDescriptorV1 } from '@mail-edge/provider';
import { ProviderControlPlaneAdapter } from '@mail-edge/provider';
import { ProviderId } from '@mail-edge/provider';
import { ProviderInstanceId } from '@mail-edge/provider';
import { ProviderReconciliationEvidenceV1 } from '@mail-edge/provider';
import { ProviderReconciliationQueryV1 } from '@mail-edge/provider';
import type { ProviderReplayIdentityV1 } from '@mail-edge/provider';
import type { RawMessageRefV1 } from '@mail-edge/provider';
import type { ReceiptId } from '@mail-edge/provider';
import { Result } from '@mail-edge/provider';
import { RouteBindingSnapshotV1 } from '@mail-edge/provider';
import type { SecretResolver } from '@mail-edge/provider';
import type { SmtpEnvelopeV1 } from '@mail-edge/provider';
import type { TenantId } from '@mail-edge/provider';

// @public
export const createResendIdempotencyHeader: (providerInstanceId: ProviderInstanceId, attemptId: AttemptId) => string;

// @public
export const createResendProviderRegistration: (config: ResendProviderConfig, dependencies: ResendProviderDependencies) => Result<ResendProviderRegistration, MailEdgeError>;

// @public
export const deriveResendIdempotencyKey: (providerInstanceId: ProviderInstanceId, attemptId: AttemptId) => string;

// @public
export const evaluateResendIdempotencyWindow: (firstUsedAt: string, now: string) => Result<"active" | "expired", MailEdgeError>;

// @public
export const inspectResendRawDownloadResponse: (statusCode: number, contentLengthHeader: string | undefined, maximumBytes: number) => ResendRawResponseInspection;

// @public
export const inspectResendRawDownloadUrl: (url: URL, allowedHosts: readonly string[]) => ResendRawUrlInspection;

// @public
export const isPublicResendAddress: (address: string) => boolean;

// @public
export class NodeResendDnsResolver implements ResendDnsResolver {
    // (undocumented)
    resolve(hostname: string, signal: AbortSignal): Promise<Result<readonly ResendResolvedAddress[], MailEdgeError>>;
}

// @public
export class NodeResendHttpTransport implements ResendHttpTransport {
    // (undocumented)
    request(input: ResendHttpRequest, signal: AbortSignal): Promise<Result<ResendHttpResponse, MailEdgeError>>;
}

// @public
export class NodeResendRawDownloadTransport implements ResendRawDownloadTransport {
    constructor(resolver?: ResendDnsResolver);
    // (undocumented)
    open(input: ResendRawDownloadRequest, signal: AbortSignal): Promise<Result<ResendRawDownloadResponse, MailEdgeError>>;
}

// @public
export class NodeResendSmtpConnector implements ResendSmtpConnector {
    // (undocumented)
    connect(input: {
        readonly host: string;
        readonly port: number;
        readonly timeoutMilliseconds: number;
    }, signal: AbortSignal): Promise<Result<ResendSmtpSession, MailEdgeError>>;
}

// @public
export const RESEND_DEFAULT_REQUESTS_PER_SECOND = 10;

// @public
export const RESEND_FEEDBACK_EVENTS: readonly ["email.sent", "email.delivered", "email.delivery_delayed", "email.bounced", "email.failed", "email.complained", "email.suppressed", "email.opened", "email.clicked"];

// @public
export const RESEND_IDEMPOTENCY_TTL_SECONDS: number;

// @public
export const RESEND_MAX_HEADER_BYTES: number;

// @public
export const RESEND_MAX_IDEMPOTENCY_KEY_BYTES = 256;

// @public
export const RESEND_MAX_MESSAGE_BYTES: number;

// @public
export const RESEND_MAX_RAW_LINE_BYTES = 1000;

// @public
export const RESEND_MAX_RECIPIENTS = 50;

// @public
export const RESEND_MAX_WEBHOOK_BYTES: number;

// @public
export const RESEND_PROVIDER_ID: ProviderId;

// @public
export const RESEND_SMTP_HOST = "smtp.resend.com";

// @public
export const RESEND_SMTP_PORT: 465;

// @public
export const RESEND_SMTP_USERNAME = "resend";

// @public
export const RESEND_WEBHOOK_TOLERANCE_SECONDS: number;

// @public
export const resendAdapterIdentity: Readonly<{
    adapterVersion: "0.1.0";
    mode: "smtp_raw";
    providerId: ProviderId;
}>;

// @public
export interface ResendControlPlane extends ProviderControlPlaneAdapter {
    // (undocumented)
    discoverDnsRecords(binding: RouteBindingSnapshotV1, signal: AbortSignal): Promise<Result<readonly ResendDnsRecord[], MailEdgeError>>;
    // (undocumented)
    requestDomainVerification(binding: RouteBindingSnapshotV1, operation: ControlPlaneOperationContext, signal: AbortSignal): Promise<Result<NormalizedEvidence, MailEdgeError>>;
}

// @public
export interface ResendDnsRecord {
    // (undocumented)
    readonly name: string;
    // (undocumented)
    readonly priority?: number;
    // (undocumented)
    readonly record: string;
    // (undocumented)
    readonly status: string;
    // (undocumented)
    readonly ttl: string;
    // (undocumented)
    readonly type: "CAA" | "CNAME" | "MX" | "TXT";
    // (undocumented)
    readonly value: string;
}

// @public
export interface ResendDnsResolver {
    // (undocumented)
    resolve(hostname: string, signal: AbortSignal): Promise<Result<readonly ResendResolvedAddress[], MailEdgeError>>;
}

// @public (undocumented)
export type ResendFeedbackEvent = (typeof RESEND_FEEDBACK_EVENTS)[number];

// @public
export interface ResendHttpRequest {
    // (undocumented)
    readonly body?: Uint8Array;
    // (undocumented)
    readonly headers: Readonly<Record<string, string>>;
    // (undocumented)
    readonly maximumResponseBytes: number;
    // (undocumented)
    readonly method: "DELETE" | "GET" | "PATCH" | "POST";
    // (undocumented)
    readonly timeoutMilliseconds: number;
    // (undocumented)
    readonly url: URL;
}

// @public
export interface ResendHttpResponse {
    // (undocumented)
    readonly body: Uint8Array;
    // (undocumented)
    readonly headers: Readonly<Record<string, string>>;
    // (undocumented)
    readonly statusCode: number;
}

// @public
export interface ResendHttpTransport {
    // (undocumented)
    request(request: ResendHttpRequest, signal: AbortSignal): Promise<Result<ResendHttpResponse, MailEdgeError>>;
}

// @public
export interface ResendInboundAcquisitionClaim {
    // (undocumented)
    readonly binding: RouteBindingSnapshotV1;
    // (undocumented)
    readonly providerInstanceId: ProviderInstanceId;
    // (undocumented)
    readonly receiptId: ReceiptId;
    // (undocumented)
    readonly receivedEmailId: string;
    // (undocumented)
    readonly schemaVersion: "v1";
    // (undocumented)
    readonly tenantId: TenantId;
}

// @public
export interface ResendInboundMetadataCommitInput {
    // (undocumented)
    readonly binding: RouteBindingSnapshotV1;
    // (undocumented)
    readonly providerId: ProviderId;
    // (undocumented)
    readonly providerInstanceId: ProviderInstanceId;
    // (undocumented)
    readonly providerReceiptKey: string;
    // (undocumented)
    readonly receivedAt: string;
    // (undocumented)
    readonly receivedEmailId: string;
    // (undocumented)
    readonly replay: ProviderReplayIdentityV1;
    // (undocumented)
    readonly schemaVersion: "v1";
    // (undocumented)
    readonly tenantId: TenantId;
    // (undocumented)
    readonly verificationEvidenceDigest: string;
}

// @public
export interface ResendInboundMetadataPort {
    // (undocumented)
    claimAcquisition(input: {
        readonly receiptId: ReceiptId;
        readonly providerInstanceId: ProviderInstanceId;
    }, signal: AbortSignal): Promise<Result<ResendInboundAcquisitionClaim, MailEdgeError>>;
    // (undocumented)
    commitAcquiredRaw(input: {
        readonly receiptId: ReceiptId;
        readonly raw: RawMessageRefV1;
        readonly envelope: SmtpEnvelopeV1;
        readonly retrievalEvidenceDigest: string;
    }, signal: AbortSignal): Promise<Result<void, MailEdgeError>>;
    // (undocumented)
    commitAuthenticatedMetadata(input: ResendInboundMetadataCommitInput, signal: AbortSignal): Promise<Result<InboundIngressCommit, MailEdgeError>>;
    // (undocumented)
    recordAcquisitionFailure(input: {
        readonly receiptId: ReceiptId;
        readonly disposition: "quarantine" | "retry_wait";
        readonly errorCode: MailEdgeError["code"];
    }, signal: AbortSignal): Promise<Result<void, MailEdgeError>>;
}

// @public
export interface ResendOutbound extends OutboundProviderAdapter {
    // (undocumented)
    reconcile(query: ProviderReconciliationQueryV1, signal: AbortSignal): Promise<Result<ProviderReconciliationEvidenceV1, MailEdgeError>>;
}

// @public
export interface ResendProviderConfig {
    readonly apiKeySecretReference: string;
    readonly feedbackPath: string;
    readonly feedbackWebhookEndpoint: string;
    readonly feedbackWebhookSecretDestination: string;
    readonly feedbackWebhookSecretReferences: readonly [string, ...string[]];
    readonly inboundBindings: readonly RouteBindingSnapshotV1[];
    readonly inboundPath: string;
    readonly inboundWebhookEndpoint: string;
    readonly inboundWebhookSecretDestination: string;
    readonly inboundWebhookSecretReferences: readonly [string, ...string[]];
    readonly maximumApiConcurrency: number;
    readonly maximumApiQueueDepth: number;
    readonly maximumRawAcquisitionConcurrency: number;
    readonly maximumRawAcquisitionQueueDepth: number;
    readonly maximumSmtpConcurrency: number;
    readonly maximumSmtpQueueDepth: number;
    readonly networkTimeoutMilliseconds: number;
    readonly rawDownloadAllowedHosts: readonly [string, ...string[]];
    readonly region: ResendRegion;
    readonly smtpEhloName: string;
    readonly webhookReplayTtlSeconds: number;
}

// @public
export interface ResendProviderDependencies {
    // (undocumented)
    readonly clock: Clock;
    // (undocumented)
    readonly httpTransport?: ResendHttpTransport;
    // (undocumented)
    readonly inboundMetadata: ResendInboundMetadataPort;
    // (undocumented)
    readonly rawDownloadTransport?: ResendRawDownloadTransport;
    // (undocumented)
    readonly secrets: SecretResolver;
    // (undocumented)
    readonly smtpConnector?: ResendSmtpConnector;
    // (undocumented)
    readonly stages: BlobStagePort;
    // (undocumented)
    readonly webhookSecretSink: ResendWebhookSecretSink;
}

// @public
export const resendProviderDescriptor: ProviderCapabilityDescriptorV1;

// @public
export interface ResendProviderRegistration extends ProviderAdapterRegistration {
    // (undocumented)
    readonly controlPlane: ResendControlPlane;
    // (undocumented)
    readonly feedback: FeedbackProviderAdapter;
    // (undocumented)
    readonly inbound: InboundProviderAdapter;
    // (undocumented)
    readonly outbound: ResendOutbound;
    // (undocumented)
    readonly rawAcquirer: InboundRawAcquirer;
}

// @public
export interface ResendRawDownloadRequest {
    // (undocumented)
    readonly allowedHosts: readonly string[];
    // (undocumented)
    readonly maximumBytes: number;
    // (undocumented)
    readonly timeoutMilliseconds: number;
    // (undocumented)
    readonly url: URL;
}

// @public
export interface ResendRawDownloadResponse {
    // (undocumented)
    readonly body: AsyncIterable<Uint8Array>;
    // (undocumented)
    readonly contentLength: number | null;
    // (undocumented)
    readonly contentType: string | null;
    // (undocumented)
    readonly headers: Readonly<Record<string, string>>;
    // (undocumented)
    readonly statusCode: number;
}

// @public
export interface ResendRawDownloadTransport {
    // (undocumented)
    open(request: ResendRawDownloadRequest, signal: AbortSignal): Promise<Result<ResendRawDownloadResponse, MailEdgeError>>;
}

// @public
export interface ResendRawResponseInspection {
    // (undocumented)
    readonly allowed: boolean;
    // (undocumented)
    readonly contentLength: number | null;
    // (undocumented)
    readonly reason: string;
}

// @public
export interface ResendRawUrlInspection {
    // (undocumented)
    readonly allowed: boolean;
    // (undocumented)
    readonly reason: string;
}

// @public
export type ResendRegion = "ap-northeast-1" | "eu-west-1" | "sa-east-1" | "us-east-1";

// @public
export interface ResendResolvedAddress {
    // (undocumented)
    readonly address: string;
    // (undocumented)
    readonly family: 4 | 6;
}

// @public
export interface ResendSmtpConnector {
    // (undocumented)
    connect(input: {
        readonly host: string;
        readonly port: number;
        readonly timeoutMilliseconds: number;
    }, signal: AbortSignal): Promise<Result<ResendSmtpSession, MailEdgeError>>;
}

// @public
export interface ResendSmtpResponse {
    // (undocumented)
    readonly code: number;
    // (undocumented)
    readonly lines: readonly string[];
}

// @public
export interface ResendSmtpSession {
    // (undocumented)
    close(signal: AbortSignal): Promise<Result<void, MailEdgeError>>;
    // (undocumented)
    readResponse(signal: AbortSignal): Promise<Result<ResendSmtpResponse, MailEdgeError>>;
    // (undocumented)
    writeCommand(command: string, signal: AbortSignal): Promise<Result<void, MailEdgeError>>;
    // (undocumented)
    writeData(chunk: Uint8Array, signal: AbortSignal): Promise<Result<void, MailEdgeError>>;
}

// @public
export interface ResendWebhookSecretSink {
    // (undocumented)
    store(destination: string, secret: Uint8Array, signal: AbortSignal): Promise<Result<void, MailEdgeError>>;
}

// @public
export const validateResendProviderConfig: (config: ResendProviderConfig) => Result<ResendProviderConfig, MailEdgeError>;

```
