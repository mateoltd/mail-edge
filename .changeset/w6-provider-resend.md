---
"@mail-edge/conformance": minor
"@mail-edge/contracts": minor
"@mail-edge/core": minor
"@mail-edge/provider-resend": minor
---

Add the production Resend provider adapter with authenticated inbound raw acquisition, strict raw
SMTP dispatch, signed feedback, conservative reconciliation, authorized domain/webhook control
operations, deterministic conformance coverage, and clean-room exports.

Allow provider conformance drivers to supply provider-derived immutable transmission objects and
multi-request feedback fixtures without weakening the provider-neutral dispatch boundary checks.
